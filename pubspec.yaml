name: maxi_library_db_sqlite
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

# Add regular dependencies here.
dependencies:
  sqlite3: ^2.4.6
  reflectable: ^4.0.9
  # path: ^1.8.0

  maxi_library:
  #git:
    #  url: https://github.com/MaxiCamilo/maxi_library.git
    #  ref: main
    path: ../maxi_library

  maxi_library_db:
  #git:
    #  url: https://github.com/MaxiCamilo/maxi_library.git
    #  ref: main
    path: ../maxi_library_db

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
